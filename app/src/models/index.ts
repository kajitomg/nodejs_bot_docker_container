import { userModel } from './user'
import { codeModel } from './code'

export enum CodeStatuses {
  'accept',
  'reject',
  'moderation',
}

export enum Games {
  'TAPSWAP',
  'XEMPIRE',
  'BLUM',
}


export {
  userModel,
  codeModel
}

export default {
  userModel,
  codeModel
}